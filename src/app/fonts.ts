import { Inter, Domine, Roboto, Open_Sans, Lato, Montserrat, Poppins, Playfair_Display } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})

export const domine = Domine({
  subsets: ['latin'],
  display: 'swap',
})

export const roboto = Roboto({
  subsets: ['latin'],
  display: 'swap',
})

export const openSans = Open_Sans({
  subsets: ['latin'],
  display: 'swap',
})

export const lato = Lato({
  weight: ['400', '700'],
  subsets: ['latin'],
  display: 'swap',
})

export const montserrat = Montserrat({
  subsets: ['latin'],
  display: 'swap',
})

export const poppins = Poppins({
  weight: ['400', '700'],
  subsets: ['latin'],
  display: 'swap',
})


export const playfairDisplay = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
})